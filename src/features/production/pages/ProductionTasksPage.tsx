import { ProductionTaskSection } from "@/features/production/components/ProductionTaskSection";

/** Full-page production task pool (`/production-tasks`). */
const ProductionTasksPage = () => <ProductionTaskSection variant="page" layout="tasksOnly" />;

export default ProductionTasksPage;
